# Clean Architecture - Telegram TimeChecker

Проект реализован на принципах **Clean Architecture (Чистой Архитектуры)** для обеспечения масштабируемости, тестируемости и независимости от внешних фреймворков.

## 📂 Слои системы (Layers)

### 1. Domain Layer (`app/domain/`)
**Ядро системы.** Не имеет внешних зависимостей.
- **Interfaces**: Определяют контракты для хранилищ, калькуляторов и репозиториев (`i_storage.py`, `i_calculator.py`). Обеспечивают независимость от реализации.
- **Entities/Calculator**: Чистая бизнес-логика, не связанная с вводом-выводом (`calculator.py`).

### 2. Use Cases Layer (`app/use_cases/`)
**Сценарии использования.** Оркестрация бизнес-логики.
- **ShiftController**: Координатор жизненного цикла смены (начало, конец, регистрация).
- **VideoUploadService**: Сервис для загрузки медиафайлов.
- **UserManager**: Логика управления пользователями.

### 3. Infrastructure Layer (`app/infrastructure/`)
**Внешние адаптеры.** Реализация интерфейсов из Domain слоя.
- **Google**: Работа с Google Sheets и Drive API.
- **Storage**: Локальные хранилища (SQLite, Excel) и композитное хранилище.

### 4. Presentation Layer (`app/presentation/`)
**Интерфейс пользователя.**
- **Telegram Bot**: Реализация бота на `aiogram`. Хендлеры, клавиатуры и управление состояниями (FSM).

## 📁 Структура папок

*   `app/` — Исходный код системы.
*   `credentials/` — Конфиденциальные данные (.env, токены, ключи API).
*   `data/` — Локальные хранилища данных (SQLite, Excel).
*   `docs/` — Техническая документация и инструкции.
*   `logs/` — Журналы работы приложения.
*   `main.py` — Точка входа.

## 🔄 Потоки данных (Data Flow)
1.  **Input**: Бот получает сообщение от пользователя.
2.  **Controller**: Вызывает нужный Use Case.
3.  **Infrastructure**: Use Case работает с данными через интерфейсы (например, сохраняет в Google Sheets).
4.  **Output**: Бот отправляет результат пользователю.

---
*Developed for Advanced Agentic Coding tasks.*
